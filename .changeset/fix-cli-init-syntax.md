---
"owosk": patch
---

Fix syntax error in `owo.config.js` generated by `init` command.

- Fix incorrect argument order when generating config with credit packs.
- Use `module.exports` for CommonJS configuration files.
- Omit TypeScript non-null assertions (`!`) in JavaScript output.
- Prevent naming collisions with internal builder functions.
