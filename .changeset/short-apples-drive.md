---
"owostack": patch 
"@owostack/types": patch
---

Add customer billing config support to the SDK, including `owo.customer` setters for per-feature overage config and customer-wide overage limits, plus enriched customer responses that include active billing config state.
