---
title: Plans
description: Configure billing plans in Owostack
icon: Layers
---

# Plans

Plans define what your customers can subscribe to. Each plan maps to a Paystack plan.

## Creating a Plan

1. Go to **Plans** in the sidebar
2. Click **Create Plan**
3. Fill in the details:

| Field       | Description                             |
| ----------- | --------------------------------------- |
| Name        | Display name (e.g., "Pro Plan")         |
| Price       | Amount in kobo (₦50 = 5000)             |
| Interval    | `monthly`, `quarterly`, `annually`      |
| Description | Optional description for your reference |

<Callout type="important">
  When you create a plan, Owostack automatically creates a corresponding plan in
  your Paystack account. The `paystackPlanId` is generated automatically.
</Callout>

## Plan Features

Define what features each plan includes by creating **Entitlements**:

```ts
// Example plan with features
{
  name: "Pro Plan",
  price: 5000, // ₦50
  features: {
    api_calls: { limit: 10000 },    // 10,000 API calls/month
    storage_gb: { limit: 50 },      // 50 GB storage
    team_members: { limit: 5 },     // Up to 5 team members
    premium_features: true,          // Boolean access
  }
}
```

## Metered vs Feature Entitlements

### Feature Entitlements (Boolean)

Simple on/off access to a capability:

```ts
await owo.check({ customer: "user@example.com", feature: "premium_features" });
// Returns: { allowed: true }
```

### Metered Entitlements (Usage-based)

Track usage against a limit:

```ts
await owo.check({ customer: "user@example.com", feature: "api_calls" });
// Returns: { allowed: true, remaining: 9500, limit: 10000 }
```

## Updating Plans

When you update a plan:

- **Price changes** affect new subscribers only
- **Feature changes** affect all subscribers immediately

## Syncing with Paystack

If you create plans directly in Paystack:

1. Go to **Plans** → **Sync from Paystack**
2. Owostack will import your existing Paystack plans
3. Configure features for each imported plan
